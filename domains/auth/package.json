{
  "name": "@voxdeck/auth",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Auth domain contracts (types + helpers). UI stays in apps.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --experimental-strip-types --test src/session-user.test.ts"
  },
  "devDependencies": {
    "@voxdeck/tsconfig": "*",
    "typescript": "^5.9.3"
  }
}
